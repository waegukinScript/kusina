doctype html
html(lang='en')
  head
     title Jade
     style.
       table{
       border-collapse: collapse;
       margin-top: 30px;
       margin-left: 70px;
       margin-right: 30px;
       }
       .heading{
       font-weight: bold;
       width: 150px;
       }
       .value{
       width: 500px;
       }
   body
     div(style='margin-top: 50px')
       h1(style='margin-left: 70px;') Registrations
       hr(style=' margin-top:0px; height:10px;border:none;color:#333;background-color:#333; margin-left: 70px; margin-right: 73px;')
       each event, index in eventsReceived
           p(style='margin-left: 70px;')= index+1
            table(border='1')
                tbody
                    tr
                        td.heading Name
                        td.value= event.name
                    tr
                        td.heading Email
                        td.value= event.email
                    tr
                        td.heading Phone Number
                        td.value= event.phoneNumber
                    tr
                        td.heading Date
                        td.value= event.date
                    tr
                        td.heading Time
                        td.value= event.time
                    tr
                        td.heading Persons
                        td.value= event.persons

